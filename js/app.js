document.addEventListener("DOMContentLoaded",()=>{const t=document.querySelector("#menu-toggle");document.querySelectorAll(".menu a").forEach(a=>a.addEventListener("click",()=>{if(t)t.checked=false}));const o=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting)e.target.classList.add("show")}),{threshold:.12});document.querySelectorAll(".reveal").forEach(e=>o.observe(e));});
function makeQR(){const value=document.getElementById("qrText")?.value.trim();const box=document.getElementById("qrcode");if(!box)return;if(!value){box.textContent="Entrez un texte ou un lien.";return}box.innerHTML="";if(window.QRCode){new QRCode(box,{text:value,width:150,height:150,correctLevel:QRCode.CorrectLevel.M});}else{box.textContent="Le générateur est en cours de chargement. Réessayez.";}}
function makeBarcode(){const value=document.getElementById("barcodeText")?.value.trim();if(!value)return;if(window.JsBarcode){JsBarcode("#barcode",value,{format:"CODE128",displayValue:true,height:65,width:2,margin:8});}}
document.addEventListener("DOMContentLoaded",()=>{const form=document.getElementById("contact-form");if(form){form.addEventListener("submit",e=>{e.preventDefault();const name=document.getElementById("name").value.trim();const phone=document.getElementById("phone").value.trim();const service=document.getElementById("service").value;const details=document.getElementById("details").value.trim();const message=`Bonjour Ghis Group,\n\nNom : ${name}\nNuméro : ${phone}\nService souhaité : ${service}\n\nProjet :\n${details}`;window.open("https://wa.me/242064573078?text="+encodeURIComponent(message),"_blank","noopener");});}});
